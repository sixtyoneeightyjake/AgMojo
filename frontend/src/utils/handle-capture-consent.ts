/**
 * Handle user consent for tracking (no-op, analytics removed)
 * @param consent Whether the user consents to tracking
 */
export const handleCaptureConsent = (_consent: boolean) => {
  // Intentionally empty
};
